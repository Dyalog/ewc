 dqTimer args;R;done;code;sns;names;i;n;e;m;timer;name;⎕TRAP
⍝ Handle one Timer event for interactive operation - called on event 1001
 R←3⊃args

 :If (⊂'∆DQ')∊⎕SI    ⍝ If we are in a ∆DQ
     done←0 ⋄ names←0⍴⊂''
     :If 'N'∊LOGMODES
         'N'Log'Message received for ',(⊃z),': ',1 ⎕JSON⍠'HighRank' 'Split'⊢R
     :EndIf
     :If MODE=2
         R.CodeLocation←⍕caller
     :EndIf
     :If 0=caller.⎕NC name←R.Event.ID
         'W'Log'Ignoring event on non-existent object ',R.Event.ID
     :Else
         names←(⊃args) ⎕WG 'Data'
         ⍝ :If ∨/((≢¨names)↑¨⊂name)∊names
         ⍝ :OrIf 'Timer'≡caller⍎name,'.Type'
         (done R)←processEvent R
         ⍝ QUEUEIDS,←⊂{(¯1+⍵⍳'.')↑⍵}R.Event.ID ⋄ QUEUEDATA,←R
         ⍝:Else
         ⍝    ⎕TRAP←0 'S' ⋄ ∘∘∘
         ⍝    'W'Log'Ignoring event on unqueued object ',name
         ⍝:EndIf
     :EndIf
     done∨←∧/0=caller.⎕NC names ⍝ All DQ'd objects gone?
     :If done
         ⎕NQ (⊃args) 1002 R ⍝ Terminate ∆DQ
     :EndIf

     ⍝:Repeat
     ⍝    :If (≢QUEUEIDS)≥i⊣n←(QUEUEIDS,⊂⍬)[i]⊣e←(QUEUEDATA,⊂⍬)[i]⊣i←⌊/QUEUEIDS⍳names
     ⍝        (QUEUEIDS QUEUEDATA)←(m←(⍳≢QUEUEIDS)≠QUEUEIDS⍳n)∘/¨QUEUEIDS QUEUEDATA
     ⍝        done←processEvent e
     ⍝    :EndIf
     ⍝:Until done

 :Else
     code←⍎sns←{(-(⌽⍵)⍳'.')↓⍵}⊃args
     :If 0≠≢R
         R.CodeLocation←{(-(⌽⍵)⍳'.')↓⍵}sns
         done←processEvent R
     :EndIf
 :EndIf
